version: '3.8'

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "40002:3000"
    environment:
      - NEXT_PUBLIC_BASE_URL=http://localhost:3000/api/v1

    restart: unless-stopped